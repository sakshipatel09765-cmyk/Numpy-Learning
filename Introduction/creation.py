#creating arrays from python lists
#we use this when we already have a list and we want to convert it into a numpy array
#np.array([ele1, ele2, ele3, ...])

import numpy as np 
arr = np.array([1,2,3,4,5])
print(arr)